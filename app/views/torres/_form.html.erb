<%= form_for(torre) do |f| %>
  <% if torre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(torre.errors.count, "error") %> prohibited this torre from being saved:</h2>

      <ul>
      <% torre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :portero %>
    <%= f.text_field :portero %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
